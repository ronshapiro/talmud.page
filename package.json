{
  "dependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "test": "jest",
    "dev": "node parcel.js dev",
    "build": "node parcel.js prod",
    "deploy": "npm run build && gcloud app deploy",
    "ignore trailing commas": "exit 1"
  },
  "devDependencies": {
    "jest": "^26.0.1"
  },
  "ignore trailing commas": "hello, world"
}
