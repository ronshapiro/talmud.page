{
  "dependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "test": "jest",
    "dev": "  rm -rf dist && parcel watch templates/talmud_page.html templates/preferences.html --no-source-maps",
    "build": "rm -rf dist && parcel build templates/talmud_page.html templates/preferences.html --no-source-maps",
    "deploy": "npm run build && gcloud app deploy"
  },
  "devDependencies": {
    "jest": "^26.0.1"
  },
  "ignore trailing commas": "hello, world"
}
